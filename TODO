


- if the cmd doesn't exist or otherwise fails, we don't flag it correctly


- might want to flush stdout at the end of the pipeline?

- provide a way to set the env variables

- probably want to set the cwd and environ of the Filter thread to
same thing that the subprocess.Popen is set to.